{
  "name": "demos-node-starter",
  "version": "0.1.0",
  "description": "Security-first command center for a single DEMOS testnet node",
  "private": true,
  "license": "CC-BY-NC-SA-4.0",
  "repository": "github:mj-deving/demos-node-starter",
  "engines": {
    "bun": ">=1.3.14"
  },
  "type": "module",
  "scripts": {
    "check": "bun run typecheck && bun test && bash -n scripts/remote-bootstrap.sh scripts/configure-secrets.sh scripts/install-secret-helpers.sh demosctl",
    "typecheck": "tsc --noEmit",
    "start": "bun src/demosctl.ts"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "typescript": "7.0.2"
  }
}
