{
  "name": "demos-node-starter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "bun run typecheck && bun test && bash -n scripts/remote-bootstrap.sh scripts/configure-secrets.sh demosctl",
    "typecheck": "tsc --noEmit",
    "start": "bun src/demosctl.ts"
  },
  "devDependencies": {
    "@types/bun": "1.4.0",
    "typescript": "7.0.2"
  }
}
